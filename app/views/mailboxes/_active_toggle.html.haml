- if mailbox.active
  = link_to(image_tag('active.png'), set_inactive_mailbox_path(mailbox), :method => :post, :remote => true)
- else 
  = link_to(image_tag('inactive.png'), set_active_mailbox_path(mailbox), :method => :post, :remote => true)
