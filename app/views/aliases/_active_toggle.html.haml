- if a.active
  = link_to(image_tag('active.png'), set_inactive_alias_path(a), :method => :post, :remote => true)
- else 
  = link_to(image_tag('inactive.png'), set_active_alias_path(a), :method => :post, :remote => true)
