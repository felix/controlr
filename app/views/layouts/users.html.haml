- content_for :sidebar do
  %h3= t('.title')
  %nav
    %ul
      %li= link_to t('.list'), users_path, :class => 'list button'
      - if can? :create, User
        %li= link_to t('.new'), new_user_path, :class => 'add button'
= render :file => 'layouts/application'
