- content_for :sidebar do
  %h3= t('.title')
  %nav
    %ul
      %li= link_to t('.list'), name_records_path, :class => 'list button'
      - if can? :create, NameRecord
        %li= link_to t('.new'), new_name_record_path, :class => 'add button'
= render :file => 'layouts/application'
