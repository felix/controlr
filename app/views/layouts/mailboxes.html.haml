- content_for :sidebar do
  %h3= t('.title')
  %nav
    %ul
      %li= link_to t('.list'), mailboxes_path, :class => 'list button'
      - if can? :create, Email
        %li= link_to t('.new'), new_mailbox_path, :class => 'add button'
= render :file => 'layouts/application'
