- if name_record.active
  = link_to(image_tag('active.png'), set_inactive_name_record_path(name_record), :method => :post, :remote => true)
- else 
  = link_to(image_tag('inactive.png'), set_active_name_record_path(name_record), :method => :post, :remote => true)
