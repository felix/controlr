defaults: &defaults
  adapter: sqlite

development:
  database: db/controlr_development.db
  <<: *defaults

  # Add more repositories
  # repositories:
  #   repo1:
  #     adapter:  postgres
  #     database: sample_development
  #     username: the_user
  #     password: secrets
  #     host:     localhost
  #   repo2:
  #     ...

test:
  database: db/controlr_test.db
  <<: *defaults
production:
  database: db/controlr_production.db
  <<: *defaults
